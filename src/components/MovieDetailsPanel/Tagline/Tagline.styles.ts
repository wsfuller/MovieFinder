import { makeStyles } from '@fluentui/react';

const useTaglineStyles = makeStyles(() => {
  return {
    root: {
      marginTop: 0,
      fontStyle: 'italic',
      textAlign: 'center',
    },
  };
});

export default useTaglineStyles;

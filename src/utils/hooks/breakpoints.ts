import { breakpoints } from '../../components/App/themes';
import { IBreakpoints } from '../../components/App/themes.types';

const useBreakpoints = (): IBreakpoints => breakpoints;

export default useBreakpoints;
